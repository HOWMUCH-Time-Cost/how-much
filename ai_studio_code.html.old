<!DOCTYPE html>
<html>
<head>
  <style>
    body { width: 250px; padding: 15px; font-family: sans-serif; }
    h2 { font-size: 16px; margin-top: 0; }
    label { display: block; margin-bottom: 5px; font-size: 12px; }
    input, select { width: 100%; margin-bottom: 15px; padding: 8px; box-sizing: border-box; }
    button { width: 100%; padding: 10px; background-color: #4CAF50; color: white; border: none; cursor: pointer; }
    button:hover { background-color: #45a049; }
    #status { margin-top: 10px; color: green; font-size: 12px; text-align: center; display: none; }
  </style>
</head>
<body>
  <h2>Time Cost Settings</h2>
  
  <label for="salary">Monthly Net Salary:</label>
  <input type="number" id="salary" placeholder="e.g. 3000">

  <label for="currency">Your Salary Currency:</label>
  <select id="currency">
    <option value="USD">USD ($)</option>
    <option value="EUR">EUR (€)</option>
    <option value="BRL">BRL (R$)</option>
  </select>

  <button id="saveBtn">Save & Apply</button>
  <div id="status">Saved! Refresh page to see changes.</div>

  <script src="popup.js"></script>
</body>
</html>